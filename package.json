{
  "name": "pdf-preview-next",
  "displayName": "vscode-pdf Next",
  "description": "Modern successor to vscode-pdf: fast, secure PDF viewing in VS Code with true dark mode, the latest PDF.js (6.2.108) in a real worker thread, live reload for LaTeX/Typst including WSL, search, outline, and reliable preview state.",
  "version": "2.4.0",
  "packageManager": "bun@1.3.13",
  "publisher": "RicardoFrantz",
  "engines": {
    "vscode": "^1.95.0",
    "node": ">=22"
  },
  "categories": [
    "Visualization"
  ],
  "keywords": [
    "pdf",
    "vscode-pdf",
    "viewer",
    "preview",
    "pdf-viewer",
    "pdf-preview",
    "pdf-reader",
    "pdfjs",
    "latex",
    "typst",
    "dark-mode",
    "dark mode",
    "night mode",
    "dark theme"
  ],
  "icon": "icon.png",
  "galleryBanner": {
    "color": "#1e2227",
    "theme": "dark"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ricardofrantz/vscode-pdf-next.git"
  },
  "bugs": {
    "url": "https://github.com/ricardofrantz/vscode-pdf-next/issues"
  },
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": true
    }
  },
  "main": "./dist/extension.js",
  "contributes": {
    "customEditors": [
      {
        "viewType": "pdf-preview-next.preview",
        "displayName": "vscode-pdf Next",
        "selector": [
          {
            "filenamePattern": "*.pdf"
          }
        ]
      }
    ],
    "commands": [
      {
        "command": "pdf-preview.openPreview",
        "title": "vscode-pdf Next: Open Preview"
      },
      {
        "command": "pdf-preview.openSource",
        "title": "vscode-pdf Next: Open Externally"
      },
      {
        "command": "pdf-preview.refreshPreview",
        "title": "vscode-pdf Next: Refresh Preview"
      },
      {
        "command": "pdf-preview.print",
        "title": "vscode-pdf Next: Open in System Viewer for Printing"
      },
      {
        "command": "pdf-preview.printDirect",
        "title": "vscode-pdf Next: Print Directly to Default Printer"
      },
      {
        "command": "pdf-preview.resetViewState",
        "title": "vscode-pdf Next: Reset View State"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "PDF Preview",
      "properties": {
        "pdf-preview.default.cursor": {
          "markdownDescription": "The default cursor tool for preview.",
          "type": "string",
          "scope": "resource",
          "default": "select",
          "enum": [
            "select",
            "hand"
          ]
        },
        "pdf-preview.default.scale": {
          "markdownDescription": "The default zoom level for preview. Allowed values are `auto`, `page-actual`, `page-fit`, `page-width`, and 1-based scale values. (`1.0` means 100%)",
          "type": "string",
          "scope": "resource",
          "default": "auto",
          "pattern": "^(\\d+(?:\\.\\d+)?|auto|page-actual|page-fit|page-width)$"
        },
        "pdf-preview.default.sidebar": {
          "markdownDescription": "Open the PDF sidebar by default when the active sidebar panel is available.",
          "type": "boolean",
          "scope": "resource",
          "default": false
        },
        "pdf-preview.default.sidebarPanel": {
          "markdownDescription": "The sidebar panel to show when the PDF sidebar opens. `pdf-preview.default.sidebar` still controls whether the sidebar opens by default.",
          "type": "string",
          "scope": "resource",
          "default": "outline",
          "enum": [
            "outline",
            "thumbnails"
          ]
        },
        "pdf-preview.default.scrollMode": {
          "markdownDescription": "The default scroll mode for preview.",
          "type": "string",
          "scope": "resource",
          "default": "vertical",
          "enum": [
            "vertical",
            "horizontal",
            "wrapped"
          ]
        },
        "pdf-preview.default.spreadMode": {
          "markdownDescription": "The default spread mode for preview.",
          "type": "string",
          "scope": "resource",
          "default": "none",
          "enum": [
            "none",
            "odd",
            "even"
          ]
        },
        "pdf-preview.reload.closeOnDelete": {
          "markdownDescription": "Close the preview when the opened PDF is deleted. Disable this for TeX-style build workflows that temporarily delete and recreate the PDF.",
          "type": "boolean",
          "default": false
        },
        "pdf-preview.reload.automatic": {
          "markdownDescription": "Automatically refresh the preview when the PDF file changes. Manual reload always works through the toolbar button, the `pdf-preview.refreshPreview` command, and Cmd/Ctrl+R.",
          "type": "boolean",
          "default": true
        },
        "pdf-preview.reload.debounceMs": {
          "markdownDescription": "Milliseconds to wait after PDF file change notifications before refreshing the preview. Increase this if your build tool writes PDFs in multiple bursts.",
          "type": "number",
          "default": 800,
          "minimum": 0,
          "maximum": 10000
        },
        "pdf-preview.appearance.theme": {
          "markdownDescription": "Preview appearance. `auto` follows VS Code colors for viewer chrome, `dark` uses dark viewer chrome while leaving PDF pages unchanged, `night` asks PDF.js to recolor rendered PDF pages for lower-eye-strain reading, `reader` renders pages with a warm e-reader sepia palette, `dark-pages` is a compatibility alias for `night`, and `inverted` applies full-page CSS inversion as a fallback for scanned or image-heavy PDFs. The toolbar button cycles Night, Reader, and Invert without landing on plain pages; Shift+click it (or set this to `auto`) to go back to plain pages.",
          "type": "string",
          "scope": "resource",
          "default": "auto",
          "enum": [
            "auto",
            "light",
            "dark",
            "night",
            "reader",
            "dark-pages",
            "inverted"
          ]
        },
        "pdf-preview.appearance.pageGap": {
          "markdownDescription": "Spacing between rendered PDF pages.",
          "type": "string",
          "scope": "resource",
          "default": "normal",
          "enum": [
            "compact",
            "normal",
            "wide"
          ]
        },
        "pdf-preview.copy.autoCopySelection": {
          "markdownDescription": "Automatically copy selected PDF text to the clipboard when the mouse button is released. Disabled by default because it writes to the clipboard without an explicit copy command.",
          "type": "boolean",
          "scope": "resource",
          "default": false
        },
        "pdf-preview.printCommand": {
          "markdownDescription": "Custom command line used only by `vscode-pdf Next: Print Directly to Default Printer`. Use `{{file}}` as a placeholder for the PDF path; when omitted, the path is appended as the last argument. The command is executed without a shell. When empty, direct printing tries the default CUPS printer, then falls back to the system viewer.",
          "type": "string",
          "scope": "resource",
          "default": "",
          "restricted": true
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "node ./esbuild.config.mjs --production",
    "compile": "tsc -p ./",
    "bundle": "node ./esbuild.config.mjs",
    "typecheck": "tsc --noEmit -p ./",
    "watch": "node ./tools/watch_build.mjs",
    "watch:tsc": "tsc -w -p ./",
    "watch:bundle": "node ./esbuild.config.mjs --watch",
    "pretest": "bun run bundle && bun run test:compat",
    "test": "node ./out/src/test/runTest.js",
    "test:compat": "bun run compile && node ./tools/check_pdfjs_runtime_compat.mjs",
    "lint": "eslint ./src",
    "format": "prettier --write \"src/**/*.ts\"",
    "format:check": "prettier --check \"src/**/*.ts\"",
    "update:pdfjs": "node ./tools/update_pdfjs.mjs",
    "package": "vsce package",
    "package:scan": "node ./tools/scan_vsix.mjs",
    "package:scan:test": "node ./tools/test_scan_vsix.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/node": "^22.19.17",
    "@types/vscode": "^1.95.0",
    "@typescript-eslint/eslint-plugin": "^8.59.0",
    "@typescript-eslint/parser": "^8.59.0",
    "@vscode/test-electron": "3.1.0",
    "@vscode/vsce": "3.9.2",
    "esbuild": "0.28.1",
    "eslint": "^9.39.4",
    "globals": "^15.15.0",
    "ovsx": "1.0.0",
    "prettier": "^3.8.3",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "tmp": "0.2.6"
  },
  "extensionKind": [
    "ui"
  ]
}
